import React from 'react';
import {HeroUIProvider} from "@heroui/react";

const AuthLayout = () => {
    return (
        <HeroUIProvider>
            <h1>AuthLayout</h1>
        </HeroUIProvider>
    );
};

export default AuthLayout;
